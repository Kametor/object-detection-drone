seed: 42

frames_dir: data/processed/frames/test

checkpoint: yolo26n.pt
conf: 0.01
imgsz: 1920
high_conf_threshold: 0.25

# Same as configs/25/27, only the verifier checkpoint differs: this one
# is EfficientNet-B0 (video-balanced sampling), trained on Colab GPU.
verifier_checkpoint: results/cascade_verifier/efficientnet_b0_verifier.pt
crop_size: 64
padding_ratio: 0.4
device: cpu

output_dir: results/cascade_score_fusion_efficientnet_b0
manifest_dir: results/manifests
jpeg_quality: 90
