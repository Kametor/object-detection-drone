opencv-python>=4.9
numpy>=1.26
pyyaml>=6.0
pycocotools>=2.0

# ultralytics runs both locally (CPU: YOLO26n zero-shot, small/nano
# inference) and on Colab (GPU: SAM3/YOLO-World labeling, model training).
ultralytics>=8.3.237

# RT-DETRv2-R18 (Tier 1 item 2): HF transformers doesn't ship an R18
# backbone via ultralytics (only rtdetr-l/x, HGNetv2 backbones) -- the
# original authors' R18 checkpoint is on the Hub via transformers.
transformers>=5.0
